import type {Config} from 'prettier';

export const qwezeyPrettierConfig: Config = {
  bracketSpacing: false,
  singleQuote: true,
  trailingComma: 'all',
  arrowParens: 'avoid',
  objectWrap: 'collapse',
};
